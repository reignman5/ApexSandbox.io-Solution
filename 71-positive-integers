public List<Integer> positiveIntegers(List<Integer> numbers) {
    List<Integer> newNumbers = numbers.clone();
    for(Integer i = (numbers.size()-1) ; i>= 0 ; i--){
        if(numbers[i]<1){
            newNumbers.remove(i);
        }
    }
    return newNumbers;
}